# AXFM 목적 선언 (2026-07-03, 오너 확정)

> AI(바이브 코딩)로 다양한 업무 솔루션을 만들고 있고 앞으로 더 만들 예정이다. 그 과정의 다섯 가지
> 어려움을 **쉽게 만들어주는 플러그인**이 AXFM 의 존재 이유다. 모든 설계·기능 결정은 이 다섯 과제로 정당화한다.

## 5대 과제와 현재 커버리지 (v0.2.0 기준)

### 과제 1. 일반 사용자가 개발을 배우기엔 아직 배울 게 많다
- **커버**: `/axfm-guide`(상태 진단 → 지금 할 일 딱 하나), `/axfm-idea`(막연한 아이디어 → 요구사항),
  5단계 여정 게이지, 생성물마다 CLAUDE.md(비개발자 배려 규칙 내장), 전문용어 한 줄 설명 원칙.
- **남은 것**: 파일럿 M3 실측(사람의 10분 스톱워치)으로 실제 초보자에게 통하는지 확인.

### 과제 2. 환경 구성과 스킬 세팅이 어렵다
- **커버**: 설치 2줄(폴더 스코프 — 해당 폴더에만 적용, 전역 오염 없음), `/axfm-new`가 환경 점검(Node/Python)
  포함 결정적 생성, `/axfm-toolbox` 추천 스킬 큐레이션, troubleshooting 증상표, npm 막힘 진단.
- **남은 것**: 관리자 1회 세팅(마켓플레이스 등록, Node/Python 설치)의 자동화·체크 스크립트는 없음
  (현재는 pilot-onboarding.md 의 수동 체크리스트).

### 과제 3. 이미 바이브 코딩으로 만들어진 솔루션들과의 인터페이스 문제 ← **최대 갭**
- **커버**: 신규 생성 솔루션끼리는 표준 통로(interface.md + 공통 함수 + 스냅샷 봉투)로 연동 성립(자동 검증됨).
- **갭**: **기존(비-AXFM) 솔루션을 편입시키는 경로가 없다.** 현재 연동은 `/axfm-new`로 만든 것끼리만 가능.
- **계획(v0.3 후보 1순위)**: `/axfm-adopt` — 기존 프로젝트에 질문 몇 개 → `axfm.json`·`axfm/interface.md` 생성
  → `sync-modules.mjs`로 공통 모듈 주입 → `register.mjs`로 등록 → 폴더 스코프 활성화. 기존 스크립트를
  전부 재사용하므로 구현 비용이 낮다.

### 과제 4. 시스템마다 디자인이 제각각이다
- **커버**: 플러그인 중앙 `design/*.md`(DESIGN.md 규격)가 단일 진실 — 모든 UI 작업 전 필독 규칙,
  토큰만 사용(임의 색·간격 금지), `export-design.mjs`가 CSS 변수로 결정적 수출, 드리프트 감지·재생성.
- **남은 것**: 현재 DESIGN.md 는 placeholder — **사내 확정 디자인으로 교체 필요**(교체 시 코드 변경 0).
  폰트(Pretendard/D2Coding) 미번들. 기존 솔루션의 디자인 소급 적용은 과제 3(adopt)과 연계.

### 과제 5. 시스템 개발에 대한 관리가 안 될 수 있다
- **커버**: 로컬 레지스트리(이 PC의 솔루션 목록·경로·버전), 솔루션별 진행 여정(progress), 모듈 버전
  드리프트 감지, `/axfm-publish` 공유 체크리스트(비밀값·데이터 유출 방지 포함).
- **갭**: **전체 현황을 한눈에 보는 수단이 없다** (솔루션별 모듈 버전·스냅샷 신선도·interface 유효성 일람).
- **계획(v0.3 후보 2순위)**: `status.mjs` — 레지스트리 순회 일람표(결정적 스크립트) + `/axfm-guide` 통합.
  팀 차원 카탈로그(누가 뭘 만들었나)는 로드맵(사내 git 조직/토픽 규약으로 시작 가능).

## 로드맵 우선순위 (목적 정렬)

| 순위 | 항목 | 대응 과제 | 비용 |
|---|---|---|---|
| 1 | `/axfm-adopt` 기존 솔루션 편입 | 과제 3 | 중 (스크립트 재사용) |
| 2 | `status.mjs` 전체 현황 일람 | 과제 5 | 소 |
| 3 | 사내 확정 DESIGN.md 교체 + 폰트 번들 | 과제 4 | 소 (오너 입력 필요) |
| 4 | 파일럿 M3 실측 → 스킬 축소(9→?) 판단 | 과제 1·2 | 사람 필요 |
| 5 | 사람 간 데이터 공유 메커니즘 | 과제 3 확장 | 대 (별도 설계) |
